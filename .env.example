# Copy this file to .env for local development or Docker.
# NEVER commit a real .env file to GitHub.

DB_USERNAME=myuser
DB_PASSWORD=your_password
DB_HOST=your-db-host.com
DB_PORT=5432
DB_NAME=retail_forecasting

# Full SQLAlchemy connection string (alternative to the pieces above)
DATABASE_URL=postgresql://myuser:your_password@your-db-host.com:5432/retail_forecasting

MODEL_PATH=models/best_model.pkl
